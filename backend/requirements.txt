fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlmodel==0.0.19
bibtexparser==1.4.0
langid==1.1.6
openpyxl==3.1.2
pandas==2.2.2
python-multipart==0.0.9
aiofiles==23.2.1
pyyaml==6.0.1
scipy==1.13.0
pytest==8.2.0
httpx==0.27.0
fpdf2==2.8.3
svglib==0.9.4
pypdf==5.6.0

# --- Grey literature retrieval (app/retrieval) ---------------------------
# trafilatura is the only main-content extractor with a published,
# peer-reviewed benchmark (Barbaresi, ACL 2021), which is what makes the
# extraction step defensible in a methods section.
# pdfminer.six rather than PyMuPDF: PyMuPDF is AGPL-3.0 and would force a
# commercial licence on redistribution. See docs/retrieval/decisions.md D2.
# All three are Apache-2.0 or MIT, so they combine into a GPL-3.0 work.
#
# The first two are pinned to the versions that produced the existing pilot
# corpus — `extractions.extractor` records "trafilatura-2.2.0" and
# "pdfminer.six-20260107" on all 358 usable documents. An upgrade changes
# extracted text, and `reextract` would then rewrite a corpus a review may
# already have quoted. Upgrade deliberately, not by resolution.
trafilatura==2.2.0
pdfminer.six==20260107
warcio==1.7.5
